<?xml version="1.0" encoding="utf-8"?>
<!--
  API production đã chạy HTTPS (Nginx + Let's Encrypt trước gateway) nên app KHÔNG
  cần ngoại lệ cleartext nào cho server thật. Chỉ mở cleartext cho các host chạy
  backend local (emulator/máy dev). Phần còn lại giữ mặc định an toàn (chỉ HTTPS).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
